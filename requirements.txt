# requirements.txt

# HTTP requests
requests==2.26.0

# Vector database client
qdrant-client==1.1.1

# Retry logic
tenacity==8.0.1

# Date and time handling (usually part of Python standard library, but listed for completeness)
python-dateutil==2.8.2

# JSON handling (usually part of Python standard library, but listed for completeness)
simplejson==3.17.6

# Type hinting support for Python 3.7+
typing-extensions==4.0.1

# Environment variable management (optional, but recommended)
python-dotenv==0.19.2

# Testing framework (for writing and running tests)
pytest==7.0.1

# Logging (usually part of Python standard library, but listed for completeness)
logging==0.4.9.6

# For creating more sophisticated CLIs (if needed)
click==8.0.3

# For working with CSV files (if needed beyond standard library csv module)
pandas==1.3.5
